{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for video game archive system",
  "main": "server.js",
  "scripts": {
    "start": "npm run build:frontend && node server.js",
    "build:frontend": "npm --prefix ../frontend run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm --prefix frontend install && npm run build:frontend"
  },
  "keywords": [
    "video games",
    "archive",
    "backend",
    "express"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.10.2"
  },
  "engines": {
    "node": ">=16.x"
  }
}
