{
  "name": "game-archive",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "dependencies": {},
  "devDependencies": {}
}
