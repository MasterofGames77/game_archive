{
  "name": "game-archive",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=14.x"
  },
  "dependencies": {}
}
